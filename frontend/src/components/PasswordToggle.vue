<template>
    <button type="button" class="pw-toggle" :class="{ 'is-active': modelValue }"
            :aria-label="modelValue ? '隐藏密码' : '显示密码'"
            @click="$emit('update:modelValue', !modelValue)">
        <!-- 睁眼（显示密码态） -->
        <svg v-if="modelValue" viewBox="0 0 24 24" fill="none"
             stroke="currentColor" stroke-width="1.8"
             stroke-linecap="round" stroke-linejoin="round">
            <path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/>
            <circle cx="12" cy="12" r="3"/>
        </svg>
        <!-- 闭眼（隐藏密码态） -->
        <svg v-else viewBox="0 0 24 24" fill="none"
             stroke="currentColor" stroke-width="1.8"
             stroke-linecap="round" stroke-linejoin="round">
            <path d="M17.94 17.94A10.07 10.07 0 0 1 12 20c-7 0-11-8-11-8a18.45 18.45 0 0 1 5.06-5.94M9.9 4.24A9.12 9.12 0 0 1 12 4c7 0 11 8 11 8a18.5 18.5 0 0 1-2.16 3.19m-6.72-1.07a3 3 0 1 1-4.24-4.24"/>
            <line x1="1" y1="1" x2="23" y2="23"/>
        </svg>
    </button>
</template>

<script setup>
defineProps({
    modelValue: { type: Boolean, default: false }
})
defineEmits(['update:modelValue'])
</script>

<style scoped>
.pw-toggle {
    width: 32px; height: 32px;
    display: flex; align-items: center; justify-content: center;
    border: none; border-radius: var(--radius-sm, 6px);
    background: transparent;
    color: var(--text-tertiary, #8A8A8A);
    cursor: pointer;
    padding: 0;
    flex-shrink: 0;
    transition:
        background 0.25s ease,
        color 0.25s ease,
        transform 0.15s ease;
}

.pw-toggle svg {
    width: 15px; height: 15px;
}

/* hover */
.pw-toggle:hover {
    color: var(--text-secondary, #616161);
}

/* 选中态：圆角矩形背景 + 主色图标 */
.pw-toggle.is-active {
    background: var(--primary-bg, rgba(0, 120, 212, 0.10));
    color: var(--primary-color, #0078D4);
}

.pw-toggle.is-active:hover {
    background: var(--primary-bg, rgba(0, 120, 212, 0.16));
}

/* 点击反馈 */
.pw-toggle:active {
    transform: scale(0.92);
}
</style>
